name: Build ShowSlate beta packages

on:
  workflow_dispatch:
  push:
    tags:
      - "v*.*.*-beta.*"

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  preflight:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v6
        with:
          node-version: 22
      - name: Validate beta tag
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          set -euo pipefail
          node tools/assert-release-tag.js beta "$GITHUB_REF_NAME"
          git fetch origin main --no-tags
          if ! git merge-base --is-ancestor HEAD origin/main; then
            echo "Beta release blocked: tagged commit is not on origin/main." >&2
            exit 1
          fi

  macos-arm64:
    needs: preflight
    runs-on: macos-15
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm test
      - name: Build Apple Silicon DMG
        run: npm run dist:mac
      - name: Verify package boot and DMG
        run: |
          build_info="$("dist-installers/mac-arm64/ShowSlate.app/Contents/MacOS/ShowSlate" --print-build-info)"
          printf '%s\n' "$build_info"
          node -e "const d=JSON.parse(process.argv[1]); if(d.dirty || d.commitFull!==process.env.GITHUB_SHA) process.exit(1)" "$build_info"
          npm run check:packaged-free -- "dist-installers/mac-arm64/ShowSlate.app/Contents/Resources/app.asar"
          hdiutil verify dist-installers/ShowSlate-*-arm64.dmg
          node node_modules/@electron/fuses/dist/bin.js read --app "dist-installers/mac-arm64/ShowSlate.app"
      - uses: actions/upload-artifact@v7
        with:
          name: macos-arm64
          path: dist-installers/ShowSlate-*-arm64.dmg
          if-no-files-found: error

  windows-x64:
    needs: preflight
    runs-on: windows-2025
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm test
      - name: Build Windows installer and portable app
        run: npm run dist:win
      - name: Verify x64 package inputs
        shell: pwsh
        run: |
          if (-not (Test-Path "dist-installers/win-unpacked/ShowSlate.exe")) { throw "Missing unpacked app" }
          if (-not (Test-Path "dist-installers/ShowSlate-Setup-*.exe")) { throw "Missing installer" }
          if (-not (Test-Path "dist-installers/ShowSlate-*-portable.exe")) { throw "Missing portable app" }
          $buildInfoJson = & "dist-installers/win-unpacked/ShowSlate.exe" --print-build-info | Out-String
          if ($LASTEXITCODE -ne 0) { throw "Packaged Windows CLI boot failed." }
          Write-Host $buildInfoJson
          $buildInfo = $buildInfoJson | ConvertFrom-Json
          if ($buildInfo.dirty -or $buildInfo.commitFull -ne $env:GITHUB_SHA) {
            throw "Packaged Windows build provenance does not match the clean workflow commit."
          }
          npm run check:packaged-free -- "dist-installers/win-unpacked/resources/app.asar"
      - uses: actions/upload-artifact@v7
        with:
          name: windows-x64
          path: |
            dist-installers/ShowSlate-Setup-*.exe
            dist-installers/ShowSlate-*-portable.exe
          if-no-files-found: error

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [preflight, macos-arm64, windows-x64]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/download-artifact@v8
        with:
          pattern: "*"
          path: release-assets
          merge-multiple: true
      - name: Generate checksums
        working-directory: release-assets
        run: sha256sum ShowSlate-* > SHA256SUMS.txt
      - name: Attest release binaries
        uses: actions/attest@v4
        with:
          subject-path: release-assets/ShowSlate-*
      - name: Publish prerelease
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mac_asset="$(find release-assets -maxdepth 1 -name 'ShowSlate-*-arm64.dmg' -print -quit)"
          windows_setup="$(find release-assets -maxdepth 1 -name 'ShowSlate-Setup-*.exe' -print -quit)"
          windows_portable="$(find release-assets -maxdepth 1 -name 'ShowSlate-*-portable.exe' -print -quit)"
          checksums="release-assets/SHA256SUMS.txt"
          notes_file="docs/RELEASE-NOTES-${GITHUB_REF_NAME#v}.md"

          test -n "$mac_asset"
          test -n "$windows_setup"
          test -n "$windows_portable"
          test -f "$checksums"
          test -f "$notes_file"

          gh release create "${GITHUB_REF_NAME}" \
            "${mac_asset}#macOS Apple Silicon installer (primary tested beta)" \
            "${windows_setup}#Windows x64 installer (experimental; physical validation needed)" \
            "${windows_portable}#Windows x64 portable app (experimental; advanced)" \
            "${checksums}#Optional SHA-256 checksums" \
            --repo "${GITHUB_REPOSITORY}" \
            --prerelease \
            --verify-tag \
            --title "ShowSlate ${GITHUB_REF_NAME} - Mac-first public beta" \
            --notes-file "$notes_file"
